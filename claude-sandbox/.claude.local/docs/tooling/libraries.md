# Libraries

For this project, we're allowed to use any and all C++ 17 standard library functions.

We're also allowed to use libraries to help with configuration file parsing (JSON), networking, and more.
Using external libraries MUST be approved first.
