# CLI Preferences

- Always use `rg` (ripgrep) instead of `grep` for searching file contents — it's faster and respects .gitignore by default.
- Always use `fd` instead of `find` for finding files — it's faster and has a simpler syntax.
